@model IEnumerable<MyWebFormApp.BLL.DTOs.ArticleDTO>


<div class="row">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Article Page</h1>
    </div>

    <div class="col-lg-12">
        <!-- Basic Card Example -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Article Page</h6>
            </div>
            <div class="card-body">
                @Html.Raw(ViewData["message"])

                <form asp-action="Search" class="form-inline">
                    <p> <input type="text" id="search" class="form-control" name="search" value="@ViewData["search"]"
                            placeholder="search article name" />
                        &nbsp;
                        <input type="submit" class="btn btn-primary btn-sm" value="Search" />
                    </p>
                </form>

                <a href="/Categories/Create" class="btn btn-success btn-sm">Create New Article</a>&nbsp;
                <table class="table table-striped">
                    <tr>
                        <th>Article ID</th>
                        <th>Title</th>
                        <th></th>
                        <th>&nbsp;</th>
                        <th>&nbsp;</th>
                    </tr>
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>@category.ArticleID</td>
                            <td>@category.Title</td>
                            <td>@category.</td>
                            <td><a asp-action="Edit" asp-route-id="@category.ArticleID" class="btn btn-warning btn-sm">edit</a></td>
                            <td><a asp-action="Delete" asp-route-id="@category.ArticleID" class="btn btn-danger btn-sm">delete</a>
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>

    </div>

</div>
